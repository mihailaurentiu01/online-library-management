enum DB {
  users = "/users.json",
  category = "/category.json",
  categoryNoJson = "/category",
  authors = "/authors.json",
  authorsNoJson = "/authors",
  books = "/books.json",
}

export default DB;
